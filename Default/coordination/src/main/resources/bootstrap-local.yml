server:
  port: 9001

spring:
  application:
    name: Baker-Coordination-Service
  cloud:
    config:
      discovery:
        enable: true
        serviceId: Baker-Static-Configure-Service
      profile: local

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8762/eureka/